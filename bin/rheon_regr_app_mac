#!/usr/bin/env bash
# SPDX-License-Identifier: MIT
# Launch rheon_regr_app without manually starting a webserver on mac.

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "${ROOT_DIR}/scripts/rheon_regr_app_mac.sh" "$@"
