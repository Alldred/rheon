#!/usr/bin/env bash
# SPDX-License-Identifier: MIT
# Compatibility shim: forward to bin/rheon_run.

set -euo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "${ROOT_DIR}/bin/rheon_run" "$@"
